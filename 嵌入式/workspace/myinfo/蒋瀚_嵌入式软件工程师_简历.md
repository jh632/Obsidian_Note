# 蒋瀚

  

男｜20 岁｜15858531984｜1626748192@qq.com  

求职意向：嵌入式软件实习生｜方向：工业控制 / 电源产品 / 通信协议 / 设备驱动

  

---

  

## 教育经历

  

**温州理工学院｜本科｜电子信息工程｜2024.09-2027.06**

  

---

  

## 实习经历

  

**Etteplan（埃特博兰）｜嵌入式软件实习生｜2026.04-2026.05**

  

参与 ESP32-S3 / ESP-IDF 工业多传感器采集与无线通信项目，以及可穿戴设备 UI 与传感器适配项目，主要负责通信验证、可靠传输、工业传感器驱动封装、设备层接口设计和显示/UI 适配。

  

- 搭建 ESP-NOW 通信测试框架，基于 RTT、吞吐率、丢包率和距离表现评估无线链路性能，并与 HTTP 通信方式进行对比，为项目通信方案选型提供测试依据。

- 基于 GBN 滑动窗口机制实现 ESP-NOW 可靠传输组件，完成序号管理、ACK 应答、发送窗口、累计确认、超时重传和窗口满处理逻辑，提升无线链路在丢包场景下的数据可靠性。

- 参与双端时间同步验证，基于 ESP-NOW 时间戳交换和类 NTP 对时思路评估同步误差，并通过多次采样筛选低延迟样本，为多传感器数据对齐提供依据。

- 负责 GERB 项目设备层开发，按 handle / ops 风格封装力传感器、ADS1015 弹簧测距、RTC、电池、LCD/背光、SD 卡等驱动接口，统一初始化、读取、在线状态和错误返回风格。

- 完成力传感器 Modbus RTU 读取逻辑，涉及 UART/RS485 通信配置、保持寄存器读取、原始数据解析、零点校准和 kN 单位换算。

- 参与 PSA 项目 LVGL UI 与传感器适配，完成 CO5300/OLED 显示驱动移植、BME280 环境数据接入和 UI 数据刷新链路梳理。

  

---

  

## 项目经历

  

### GERB 工业多传感器采集与无线通信系统｜ESP32-S3 / ESP-IDF / Modbus / ESP-NOW｜2026.04-2026.05

  

项目面向工业多传感器数据采集与无线传输场景，基于 ESP-NOW、Modbus RTU、I2C、UART、SPI 等技术实现通信协议、设备驱动和数据采集链路。

  

- 设计 GBN 可靠传输组件接口，定义固定包头与窗口状态结构，支持发送窗口管理、ACK 处理、累计确认、超时轮询重传和在途包统计。

- 封装力传感器 Modbus RTU 读取逻辑，完成 UART/RS485 参数配置、保持寄存器读取、CRC 校验、原始值解析、零点校准和 kN 单位换算。

- 封装 ADS1015 弹簧测距驱动，完成 I2C 通信、配置寄存器设置、通道选择、原始 ADC 数据读取和零点偏移处理。

- 完成 RTC、电池、LCD/背光、SD 卡等设备驱动调试，统一设备初始化、状态检测、错误返回和日志输出格式，提升模块复用性与集成效率。

- 基于 FreeRTOS 任务、队列和定时机制组织通信、采集与超时处理流程，降低通信回调与业务逻辑之间的耦合。

  

---

  

### PSA 可穿戴设备固件 UI 与传感器适配｜ESP32 / ESP-IDF / LVGL｜2026.05

  

项目面向可穿戴健康监测设备，基于 ESP-IDF 与 LVGL 实现显示驱动适配、传感器数据管理和 UI 状态刷新。

  

- 完成 LVGL 与 CO5300/OLED 显示驱动适配，封装 app_ui 对外接口，将时间、连接状态、健康数据、环境数据、运动姿态和系统状态抽象为统一 UI 数据结构。

- 完成 BME280 环境传感器适配，梳理数据从 driver 层到 UI 层的刷新链路，支持温度、湿度、气压等数据展示。

- 基于 LVGL top layer 实现启动进度提示，维护蓝牙、WiFi、电池、USB 等状态栏图标刷新逻辑，并根据客户反馈调整 UI 显示内容。

  

---

  

### STM32 四轴飞控实验项目｜STM32 / MPU6050 / NRF24L01｜2025.08-2025.12

  

基于 STM32 搭建四轴飞控实验平台，完成无线遥控、姿态采集、姿态解算和基础 PID 控制验证，主要用于学习嵌入式控制系统、传感器融合和无线通信。

  

- 基于 NRF24L01 实现遥控指令下发与飞控状态回传，完成 SPI 通信、数据包解析和连接状态判断。

- 基于 MPU6050 完成加速度计与陀螺仪数据采集，使用卡尔曼滤波进行姿态角估算。

- 实现基础姿态 PID 控制与四轴电机混控输出，完成低空手动飞行和姿态稳定性调试。

- 封装传感器与通信模块接口，统一 I2C/SPI 外设访问方式，提高代码复用性和调试效率。

  

---

  

## 专业技能

  

### 嵌入式开发

  

- 熟悉 C 语言嵌入式开发，了解指针、结构体、函数指针、模块化封装、链表、队列、状态机、环形缓冲区等常用工程实现。

- 熟悉 ESP-IDF 开发流程，具备 ESP32 / ESP32-S3 项目开发经验，了解 FreeRTOS 多任务、队列、定时器、任务通知和同步机制。

- 了解 C++11 与 STL 基础用法，能够阅读英文芯片手册、技术文档和开源项目资料。

  

### 通信协议与外设

  

- 熟悉 UART、I2C、SPI、CAN、USB 等常见外设接口，具备 RS485、Modbus RTU、ESP-NOW、HTTP 等通信协议调试经验。

- 熟悉 ESP-NOW 通信链路测试与可靠传输机制，了解滑动窗口、ACK、超时重传、累计确认等协议设计思路。

- 具备传感器与外设驱动开发经验，接触过力传感器、ADS1015、BME280、RTC、电池管理、LCD/背光、SD 卡等模块的驱动封装与调试。

  

### 工程能力

  

- 熟悉基于芯片手册和寄存器说明进行驱动调试的流程，能够结合日志、串口数据、示波器波形和硬件现象定位问题。

- 了解实时固件常见设计方式，包括周期任务、状态机、超时保护、故障状态处理和模块化日志输出。

- 熟悉 LVGL UI 开发，了解对象管理、事件处理、样式系统、显示驱动适配和 UI 数据接口封装。
- 熟练使用claude/codex等主流ai程序辅助开发

  

---

  

## 荣誉奖项

  

- 全球人工智能算法大赛浙江省二等奖

- 蓝桥杯程序设计大赛浙江省二等奖

  

---

  

## 个人优势

  

关注嵌入式系统开发，对 RTOS、工业通信、传感器驱动、实时控制和电源产品固件方向有持续兴趣。能够结合芯片手册、开源代码、日志信息和实际硬件现象定位问题，具备较强的独立调试能力和工程落地意识。对 TI C2000、UPS、电源控制、ADC 采样、PWM 输出、状态机和故障保护等方向有学习兴趣，希望在工业级嵌入式项目中进一步提升实时控制与产品级固件开发能力。