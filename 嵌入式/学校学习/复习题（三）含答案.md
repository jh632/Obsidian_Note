# 复习题（三）· 含参考答案

> 题目来源：附录D——复习题（3）.pdf；答案来源：二维码——复习题（三）答案.pdf

## 一、填空题

1. CCS 汇编器输出的（**目标**）文件和链接器输出的（**可执行**）文件都是公共目标文件格式。
2. 使能 CPU 的全局中断 EINT，所对应的汇编指令（**asm(" CLRC INTM")**）。
3. ADC 将模拟量转化为数字量通常要经过 4 个步骤：（**采样、保持、量化和编码**）。
4. ADC 接口被建立在一个快速、（**12**）位 ADC 模块上，此模块在 25MHz ADC 时钟上的快速转换高达 80ns。
5. F28335 的 ADC 模块主要特点：（**12**）位模数转换、（**2**）个采样保持器、（**同步或顺序采样**）。模拟电压输入范围（**0~3**）V，ADC 转换频率最高可配置为（**25**）MHz，采样带宽（**12.5**）MHz，通道模拟输入。
6. F28335 片上外设丰富，其中有 18 路 PWM，（**6**）路 CAP，（**2×8**）通道，（**12**）位 ADC，（**88**）路 GPIO。
7. F28335 中有（**6**）组互补对称的脉宽调制 PWM，每组中包括（**2**）路 PWM，每一组中有 6 个单元：（**时钟基准子模块 TB**），（**计数比较子模块 CC**），（**动作限定子模块 AQ**），（**死区控制子模块 DB**），（**错误控制子模块 TZ**），（**事件触发子模块 ET**）。
8. 设系统时钟为 150MHz，若要产生频率为 10kHz、占空比为 50% 的 PWM 波形，则在增减计数模式下时基周期寄存器 TBPRD＝（**7500**）、计数比较寄存器 CMPA＝（**3750**）。
9. 当 ePWM 工作于（**连续递增**）计数模式时，通用定时器会产生一个非对称波形。
10. 串行总线与并行总线相比，具有较少的控制和数据线的优点，因而得到广泛应用。串行接口可分为三种，分别为（**单总线、双总线和多总线**）接口。SPI 属于（**多总线**）接口，是一个（**高速**）、（**同步**）串行 I/O 端口。
11. TMS320F28335 串行端口外设有（**2**）个 CAN 模块，（**2**）个 UART 模块，（**2**）个 McBSP 模块，（**1**）个 SPI 模块，（**1**）个 I2C 总线。
12. SCI 接收器和发射器是（**双缓冲**）缓冲的，其发送引脚是（**SCITXD**），输入引脚是（**SCIRXD**）。
13. I2C 模块数据传输速率从（**10kbps**）到（**400kbps**）。
14. 在使用 CCS 进行调试时，在 debug 下拉菜单有 "reset CPU"、"restart"、"go main" 功能的选项，执行后程序分别跳转到（**0x3FF9CE**）、（**_c_int00**）、（**主函数入口地址**）。
15. CMPRx、ADCTRL1、TxPR、GPxSET、DBTCONA、EVBIMRA、SCICCR、ADCREFSEL 寄存器中自带"影子"寄存器的是：（**CMPRx、TxPR**）。

## 二、选择题

1. 当工作于_____递增计数模式时，通用定时器会产生一个非对称波形。（**A**）
   - A.连续　B.断续　C.中断
2. F28335 片上外设丰富，其中有 18 路 PWM，_____路 CAP。（**C**）
   - A.7　B.9　C.6
3. 关于 F28335 中 AD 采样下列说法不正确的是_____。（**B**）
   - A.16 个采样通道　B.最高采样速率同 2812 一致　C.4 种工作模式
4. 设系统时钟为 150MHz，若要产生频率为 10kHz、占空比为 50% 的 PWM 波形，则在增减计数模式下时基周期寄存器 TBPRD＝7500、计数比较寄存器 CMPA＝_____。（**A**）
   - A.3750　B.3780　C.3690
5. 关于通讯，下列说法不正确的是_____。（**C**）
   - A.SCI、SPI、I2C 的速率使用波特率来描述
   - B.理论上 SCI 的通讯速率小于 SPI
   - C.常见的 SPI 通讯速率为 115200
6. McBSP 模块属于_____通讯方式。（**C**）
   - A.单工　B.半双工　C.全双工
7. 以下属于半双工通讯的是_____。（**B**）
   - A.SPI　B.I2C　C.SCI　D.CAN
8. 增强型控制器局域网（eCAN）模块有_____个邮箱。（**B**）
   - A.16　B.32　C.64
9. McBSP 模块可以与_____兼容器件一起工作。（**C**）
   - A.IIC　B.CAN　C.SPI
10. SPI 模块中外部引脚 SPICLK 是 SPI_____引脚。（**B**）
    - A.发送使能　B.串行时钟　C.从器件输入/主器件输出
11. 以下属于半双工通讯的是_____。（**B**）
    - A.SPI　B.IIC　C.SCI
12. 通信报文的优先级由标识符决定，标识符数值越小，优先级越_____。（**C**）
    - A.不影响　B.低　C.高
13. McBSP 模块不可以以_____开头的 8 位数据传输。（**C**）
    - A.LSB　B.MSB　C.HSB
14. Flash 引导程序入口地址是_____。（**A**）
    - A.0x33 FFF6　B.0x33 FFF7　C.0x33 FFF9
15. 程序计数器 PC（22 位）的复位地址为_____。（**B**）
    - A.0x33 FFF6　B.0x3F FFC0　C.0x3F F9CE　D.0x00 0000
16. 关于存储器的描述，下列说法正确的是_____。（**D**）
    - A.DSP 片上的 FLASH 只能存放程序，不能存放数据；
    - B.片上的 RAM 区只能存放数据，不能存放程序；
    - C.用户可使用片上 FLASH 的所有区间；
    - D.以上说法都不正确
17. F28335 中的数据类型下列说法正确的是_____。（**C**）
    - A.char 为 8 位，int 为 16 位　B.char 为 8 位，int 为 8 位
    - C.char 为 16 位，int 为 16 位　D.char 可以为 8 位也可以为 16 位，int 为 16 位
18. 下列描述正确的是_____。（**B**）
    - A.伪指令存在于可执行文件中　B.伪指令仅仅用于指导程序的编译
    - C.伪指令不能存在主函数中　D.以上说法都不对
19. `typedef interrupt void (*PINT)(void);` 中的 PINT 的描述不正确的是_____。（**D**）
    - A.PINT 无参数调用　B.为指向 interrupt 型函数的指针
    - C.为指针指向函数　D.以上描述不一定都对
20. Flash 引导程序入口地址是_____。（**A**）
    - A.0x33 FFF6　B.0x33 FFF7　C.0x33 FFF8　D.0x33 FFF9
21. 指令 `GpioDataRegs.GPATOGGLE.bit.GPIO0 = 0` 的含义正确的是_____。（**D**）
    - A.将该 IO 口置 1　B.将该 IO 口置 0　C.将该 IO 口电平翻转　D.无效指令
22. 关于 DSP 的程序运行，下列描述不正确的是_____。（**C**）
    - A.DSP 的主函数是一个死循环
    - B.中断函数运行时间可以与主函数运行时间一致
    - C.同单片机不同的是，DSP 的程序是顺序执行的
    - D.以上描述不一定都对

> 参考答案速查：ACBAC、CBBCB、BCCAB、DCBDA、DC

## 三、补全程序

### 1. CPU Timer0 相关寄存器的设置：假设时钟为 120MHz

**(1) 定时器的时间是多少（写出计算过程）？**

```c
CpuTimer0Regs.TPRH.all = 0;
CpuTimer0Regs.TPR.all  = 0x095F;      // 2399
CpuTimer0Regs.PRD.all  = 4999;
CpuTimer0Regs.TCR.bit.TSS = 0;
CpuTimer0Regs.TCR.bit.TRB = 0;
CpuTimer0Regs.TCR.bit.all = 0x4001;
```

**答：**

```
T = (2399+1) × (4999+1) / 120MHz = 2400 × 5000 / 120MHz = 0.1s
```

**(2) 提供一种改写的方法（定时时间保持 0.1s 不变）：**

```c
CpuTimer0Regs.TPR.all  = 0x04AF;      // 1199
CpuTimer0Regs.PRD.all  = 9999;
// T = (1199+1) × (9999+1) / 120MHz = 1200 × 10000 / 120MHz = 0.1s
```

### 2. 已知 ADC 模块按照 A3、A7、A2、A4、B2、B0 的顺序采集 6 个通道，请根据注释补全 ADC 初始化程序

```c
AdcRegs.ADCTRL3.bit.SMODE_SEL = 0x00;      // 顺序采样模式
AdcRegs.ADCtrl1.bit.SEQ_CASC  = 0x01;      // 级联模式
AdcRegs.ADCMAXCONV.all        = 0x05;      // 6 个通道（MAX_CONV = 5）
AdcRegs.ADCCHSELSEQ1.bit.CONV00 = 0x3;     // A3
AdcRegs.ADCCHSELSEQ1.bit.CONV01 = 0x7;     // A7
AdcRegs.ADCCHSELSEQ1.bit.CONV02 = 0x2;     // A2
AdcRegs.ADCCHSELSEQ2.bit.CONV03 = 0x4;     // A4
AdcRegs.ADCCHSELSEQ2.bit.CONV04 = 0xA;     // B2
AdcRegs.ADCCHSELSEQ2.bit.CONV05 = 0x8;     // B0
```
