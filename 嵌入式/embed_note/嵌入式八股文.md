## 1
MCU 上电后 main 函数之前发生了什么？
```
上电复位
↓
从复位向量取启动地址
↓
执行启动文件 startup
↓
初始化栈指针
↓
拷贝 .data 段到 RAM
↓
清零 .bss 段
↓
SystemInit，配置时钟
↓
调用 main
```

|段|含义|
|---|---|
|`.text`|代码段，通常在 Flash|
|`.rodata`|只读常量|
|`.data`|已初始化全局/静态变量，运行在 RAM，初值在 Flash|
|`.bss`|未初始化全局/静态变量，启动时清零|
|heap|动态分配区|
|stack|函数调用栈|
